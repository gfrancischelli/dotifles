@import 'main';

$primary: #26CDA4;
$grey: #444444;
$black: #000000;
$bg: #EBEBEB;
$light-gray: #707070;
$white: #FCFCFC;

body {
    max-width: 920px;
    margin: 0 auto;
    margin-top: 30px;
}

h2 {
    color: #000000;
    font-weight: 300;
    letter-spacing: 3px;
}

.pre-header {
    font-size: 3px;
    color: $bg;
}

.header {
    padding: 22px;
    color: $white;
    line-height: 1.6;
    font-size: 16px;
    background-color: $black;
    span {
        font-weight: bold;
        font-size: 36px;
    }
    img {
        vertical-align: bottom;
        width: 190px;
    }
}

.iconset {
    width: 100%;
}

.icon {
    display: inline-block;
    margin: 0;
    height: 42px;
    border-top: 3px solid;
    vertical-align: top;

    &.js { border-color: #F7DF1E}
    &.html { border-color: #E34F26 }
    &.sass { border-color: #CC6699 }
    // & { border-color:  }
    // & { border-color:  }
    // & { border-color:  }
    // & { border-color:  }
    // & { border-color:  }
}

.circle {
    display: inline-block;
    color: $primary;
}
